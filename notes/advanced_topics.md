# Advanced Topics

- **Cron Jobs**: Automate scripts with cron by adding tasks to `crontab`.
- **Process Management**: Use commands like `ps`, `top`, and `kill`.
- **Permissions**: Use `chmod` to set file permissions.
- **Networking**: Commands like `ping`, `netstat`, and `ifconfig`.
