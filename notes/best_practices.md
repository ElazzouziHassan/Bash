# Scripting Best Practices

- Use comments to explain code.
- Consistently use indentation.
- Test scripts on different environments.
- Use functions to organize reusable code.
- Use `set -e` to exit on errors for safety.
